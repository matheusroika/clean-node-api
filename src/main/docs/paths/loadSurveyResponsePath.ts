export const loadSurveyResponsePath = {
  get: {
    tags: ['Survey Response'],
    summary: 'API para consultar a resposta de um usuário em uma enquete',
    security: [{
      apiKeyAuth: []
    }],
    parameters: [{
      in: 'path',
      name: 'surveyId',
      required: true,
      schema: {
        type: 'string'
      }
    }],
    responses: {
      200: {
        description: 'Sucesso',
        content: {
          'application/json': {
            schema: {
              $ref: '#/schemas/surveyResponse'
            }
          }
        }
      },
      403: {
        $ref: '#/components/forbidden'
      },
      500: {
        $ref: '#/components/serverError'
      }
    }
  }
}
